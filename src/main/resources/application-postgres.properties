OSTGRES_HOST=localhost
POSTGRES_PORT=5432
DB_USER=atsd
DB_PASSWD=atsd

spring.datasource.url=jdbc:postgresql://${POSTGRES_HOST}:${POSTGRES_PORT}/atsd?options=-c%20TimeZone=UTC
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWD}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect